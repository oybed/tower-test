---

- hosts: notification_host
  tasks:
    - debug:
        msg: "{{ hostvars }}"
    - shell: "tree {{ inventory_dir }}"
    - include_vars:
        file: "{{ content_file }}"
    - debug:
        msg: "{{ body }}"
