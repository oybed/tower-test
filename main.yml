---

- hosts: localhost
  tasks:
    - name: Hello
      setup:
      register: hello_output
      environment:
        ANSIBLE_ROLES_PATH: "{{ '/tmp/obedin-test:' + ansible_env[ANSIBLE_ROLES_PATH]|default('') }}"
    - debug:
        msg: "{{ hello_output }}"

